# Визуальное представление архитектуры Input/Output

## 📊 Диаграмма классов (упрощённая)

```
┌─────────────────────────────────────────────────────────────────┐
│                    СИСТЕМА ВВОДА/ВЫВОДА                        │
└─────────────────────────────────────────────────────────────────┘

                          ┌──────────────┐
                          │   IOFacade   │ ◄─── Главный фасад
                          └──────┬───────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
              ┌─────▼─────┐  ┌──▼──────┐  ┌─▼──────────┐
              │   Input   │  │ Output  │  │  Адаптеры  │
              └───────────┘  └─────────┘  └────────────┘


═══════════════════════════════════════════════════════════════════
                         СЛОЙ INPUT
═══════════════════════════════════════════════════════════════════

                      ┌──────────────────┐
                      │ IInputProvider   │ ◄─── Базовый интерфейс
                      │ (контракт)       │
                      └──────────────────┘
                             △
                             │ implements
                             │
        ┌────────┬───────────┼───────────┬────────┐
        │        │           │           │        │
   ┌────┴─┐ ┌───┴────┐ ┌────┴───┐ ┌─────┴──┐ ┌──┴─────┐
   │IText │ │INumeric│ │IPassword│ │IButton │ │(extends)
   │Input │ │ Input  │ │ Input   │ │ Input  │ │        │
   └────┬─┘ └───┬────┘ └────┬───┘ └─────┬──┘ └────────┘
        │       │           │           │
        │       ▼           │           │
        └───► ConsoleInput ◄┼───────────┘
                △ △ △       
        Реализует все Input интерфейсы


═══════════════════════════════════════════════════════════════════
                        СЛОЙ OUTPUT
═══════════════════════════════════════════════════════════════════

                      ┌──────────────────┐
                      │ IOutputProvider  │ ◄─── Базовый интерфейс
                      │ (контракт)       │
                      └──────────────────┘
                             △
                             │ extends
                             │
                      ┌──────┴──────────┐
                      │ IColoredOutput  │
                      └──────┬──────────┘
                             △
                             │ extends
                             │
                      ┌──────┴──────────┐
                      │  IErrorOutput   │
                      └──────┬──────────┘
                             △
                             │
                      ┌──────┴──────────┐
                      │ ConsoleOutput   │
                      │ (реализация)    │
                      └─────────────────┘


═══════════════════════════════════════════════════════════════════
                         СЛОЙ АДАПТЕРОВ
═══════════════════════════════════════════════════════════════════

Специализированные адаптеры для удобства использования:

    ┌─────────────────┐     ┌─────────────────┐
    │  InputAdapter   │     │ OutputAdapter   │
    │ (комбинирует    │     │ (комбинирует    │
    │  все Input)     │     │  все Output)    │
    └─────────────────┘     └─────────────────┘
            △                        △
            │                        │
            └────────┬───────────────┘
                     │
              ┌──────▼────────┐
              │   IOFacade    │ ◄─── Главный фасад
              │ (удобство)    │
              └───────────────┘
```

## 🔄 Поток работы

### Сценарий 1: Простой диалог через IOFacade

```
Пользователь
    │
    ▼
┌─────────────────┐
│   IOFacade      │
├─────────────────┤
│ ShowInfo()      │ ──► ┌──────────────┐
│ AskText()       │ ──► │ ConsoleInput │ ──► Получить от пользователя
│ AskNumeric()    │     │ ConsoleOutput│ ──► Показать пользователю
│ ShowSuccess()   │ ◄── └──────────────┘
└─────────────────┘
    │
    ▼
Результат
```

### Сценарий 2: Специализированная работа с Input

```
Приложение
    │
    ▼
┌─────────────────────────────┐
│  ConsoleInput               │
├─────────────────────────────┤
│ implements ITextInput       │ ──► Получить текст
│ implements INumericInput    │ ──► Получить число
│ implements IPasswordInput   │ ──► Получить пароль
│ implements IButtonInput     │ ──► Получить выбор
└─────────────────────────────┘
    │
    ▼
Данные от пользователя
```

### Сценарий 3: Специализированная работа с Output

```
Приложение
    │
    ▼
┌─────────────────────────────┐
│  ConsoleOutput              │
├─────────────────────────────┤
│ WriteText()                 │ ──► Вывести текст
│ WriteSuccess() (зелень)     │ ──► Успех
│ WriteWarning() (жёлть)      │ ──► Предупреждение
│ WriteError() (красный)      │ ──► Ошибка
│ WriteDetailedException()    │ ──► Подробная ошибка
└─────────────────────────────┘
    │
    ▼
Вывод в консоль
```

## 📦 Зависимости интерфейсов

```
IOutputProvider (базовый уровень)
         │
         ├─► WriteText()
         ├─► WriteLine()
         └─► WriteEmptyLine()

IColoredOutput (средний уровень, extends IOutputProvider)
         │
         ├─► WriteColoredMessage()
         ├─► WriteColoredLine()
         ├─► WriteSuccess()      ◄─── Зелёный
         ├─► WriteWarning()      ◄─── Жёлтый
         └─► WriteInfo()         ◄─── Голубой

IErrorOutput (высокий уровень, extends IColoredOutput)
         │
         ├─► WriteError()
         ├─► WriteException()
         ├─► WriteDetailedException()
         └─► WriteErrorLines()
```

## 🎓 Паттерны использования

### Паттерн 1: Использование только нужного интерфейса

```csharp
// Мне нужна только текстовая информация из input
ITextInput textInput = new ConsoleInput();
string name = textInput.GetShortText("Имя:");

// Но я могу использовать и другие если надо
INumericInput numericInput = (ConsoleInput)textInput;
int age = numericInput.GetNumeric("Возраст:");
```

### Паттерн 2: Инъекция зависимостей

```csharp
public class UserService
{
    private readonly IInputProvider _input;
    private readonly IErrorOutput _output;

    // Передаём интерфейсы в конструктор
    public UserService(IInputProvider input, IErrorOutput output)
    {
        _input = input;
        _output = output;
    }

    public void Register()
    {
        try
        {
            string name = _input.GetText("Имя:");
            _output.WriteSuccess($"Добро пожаловать, {name}!");
        }
        catch (Exception ex)
        {
            _output.WriteDetailedException(ex);
        }
    }
}

// Использование
var service = new UserService(
    new ConsoleInput(),
    new ConsoleOutput()
);
service.Register();
```

### Паттерн 3: Фасад для удобства

```csharp
// Просто и удобно
var io = new IOFacade();
string answer = io.AskText("Ответ:");
io.ShowSuccess("Готово!");
```

## 🔐 Изоляция слоёв

```
┌────────────────────────────────────────────────────┐
│                    Приложение                      │
├────────────────────────────────────────────────────┤
│         (использует IOFacade или отдельные        │
│              интерфейсы Input/Output)             │
├────────────────────────────────────────────────────┤
│ Presentation/Adapters (IOFacade, Adapters)        │
├────────────────────────────────────────────────────┤
│ Presentation/Input (ConsoleInput)                 │
│ Presentation/Output (ConsoleOutput)               │
├────────────────────────────────────────────────────┤
│ Presentation/Input/Button, Text, Numeric etc.    │
│ (низкоуровневые операции)                        │
├────────────────────────────────────────────────────┤
│                 System.Console                     │
│              (консоль операционной системы)       │
└────────────────────────────────────────────────────┘
```

## 💪 Преимущества архитектуры

```
┌─────────────────────────────────────────────────┐
│ 1. РАЗДЕЛЕНИЕ ОТВЕТСТВЕННОСТИ                   │
│    Input не знает про Output и наоборот        │
├─────────────────────────────────────────────────┤
│ 2. РАСШИРЯЕМОСТЬ                               │
│    Можно добавлять новые реализации            │
│    FileOutput, GUIInput, DatabaseOutput и т.д. │
├─────────────────────────────────────────────────┤
│ 3. ТЕСТИРУЕМОСТЬ                               │
│    Интерфейсы позволяют создавать mock-ы      │
├─────────────────────────────────────────────────┤
│ 4. ГИБКОСТЬ                                    │
│    Выбирай что нужно: фасад или интерфейсы   │
├─────────────────────────────────────────────────┤
│ 5. КОНСИСТЕНТНОСТЬ                             │
│    Один стиль Input/Output по всему приложению│
└─────────────────────────────────────────────────┘
```

## 🚀 Примеры расширения

### Создание FileOutput

```csharp
public class FileOutput : IErrorOutput
{
    private readonly string _filePath;

    public FileOutput(string filePath) => _filePath = filePath;

    public void WriteText(string message) => File.AppendAllText(_filePath, message);
    public void WriteLine(string message) => File.AppendAllText(_filePath, message + Environment.NewLine);
    // ... другие методы
}

// Использование
var output = new FileOutput("log.txt");
output.WriteSuccess("Это будет записано в файл!");
```

### Создание GUIInput

```csharp
public class GUIInput : IButtonInput
{
    public bool GetYesNoChoice(string prompt)
    {
        // Показать диалог WinForms/WPF
        return MessageBox.Show(prompt, "", MessageBoxButtons.YesNo) == DialogResult.Yes;
    }

    // ... другие методы
}
```

## 📈 Метрики архитектуры

| Метрика | Значение |
|---------|----------|
| Интерфейсов Input | 5 |
| Интерфейсов Output | 3 |
| Реализаций InterfacesInput | 1 (ConsoleInput) |
| Реализаций Output | 1 (ConsoleOutput) |
| Адаптеров | 3 (IOFacade, InputAdapter, OutputAdapter) |
| Файлов документации | 4 |
| Примеров использования | 12+ |
| Отличных методов | 50+ |
