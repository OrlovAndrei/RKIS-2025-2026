# 🎨 Presentation Layer - Слой представления

Слой представления отвечает за **ввод и вывод информации** в консольном приложении.

## 📋 Быстрый старт

### Установка и использование

```csharp
using Presentation.Adapters;

// 1. Создаём фасад
var io = new IOFacade();

// 2. Выводим информацию
io.ShowInfo("Добро пожаловать!");

// 3. Получаем данные от пользователя
string name = io.AskText("Введите ваше имя:");
int age = io.AskNumeric("Введите возраст:");

// 4. Выводим результат
io.ShowSuccess($"Благодарим, {name}! Вам {age} лет.");
```

## 📁 Структура слоя

### Input (Ввод информации)
Отвечает за получение данных от пользователя:
- **ITextInput** - ввод текста (одна или несколько строк)
- **INumericInput** - ввод чисел с ограничениями
- **IPasswordInput** - ввод пароля с маскированием
- **IButtonInput** - выбор вариантов и кнопки

**Реализация:** `ConsoleInput` - полная реализация для консоли

### Output (Вывод информации)
Отвечает за вывод данных пользователю:
- **IOutputProvider** - простой вывод текста
- **IColoredOutput** - цветной вывод (успех, ошибка, предупреждение)
- **IErrorOutput** - вывод ошибок и исключений

**Реализация:** `ConsoleOutput` - полная реализация для консоли

### Adapters (Адаптеры)
Удобные обертки для различных сценариев:
- **IOFacade** - главный фасад для одновременной работы с Input и Output *(рекомендуется)*
- **InputAdapter** - для работы только с вводом
- **OutputAdapter** - для работы только с выводом

## 🎯 Три способа использования

### Способ 1: IOFacade (рекомендуется) 🌟

Самый удобный способ для большинства задач:

```csharp
var io = new IOFacade();

// Вывод
io.ShowInfo("Информация");
io.ShowSuccess("Успех!");
io.ShowWarning("Предупреждение!");
io.ShowError("Ошибка!");

// Ввод
string text = io.AskText("Текст:");
int number = io.AskNumeric("Число:");
bool agree = io.AskYesNo("Согласны?");
```

### Способ 2: Прямое использование интерфейсов

Для специализированной работы:

```csharp
ITextInput input = new ConsoleInput();
IColoredOutput output = new ConsoleOutput();

string name = input.GetNonEmptyText("Имя: ");
output.WriteSuccess($"Привет, {name}!");
```

### Способ 3: Адаптеры

Для разделения функциональности:

```csharp
// Только ввод
var input = new InputAdapter();
string text = input.GetNonEmptyText("Текст: ");

// Только вывод
var output = new OutputAdapter();
output.WriteSuccess("Готово!");
```

## 📚 Основные компоненты

### ConsoleInput
Получение информации от пользователя:

```csharp
var input = new ConsoleInput();

// Текст
string name = input.GetShortText("Имя: ");

// Числа
int age = input.GetNumericInRange("Возраст: ", 18, 100);

// Пароль
string password = input.GetPassword("Пароль: ");

// Выбор
bool choice = input.GetYesNoChoice("Согласны?");
```

### ConsoleOutput
Вывод информации:

```csharp
var output = new ConsoleOutput();

// Простой вывод
output.WriteLine("Текст");

// Цветной вывод
output.WriteSuccess("Успех!");     // зелёный
output.WriteWarning("Внимание!");  // жёлтый
output.WriteError("Ошибка!");      // красный
output.WriteInfo("Информация");    // голубой

// Вывод ошибок
try { /* код */ }
catch (Exception ex)
{
    output.WriteDetailedException(ex);
}
```

### OutputAdapter
Дополнительные удобства:

```csharp
var output = new OutputAdapter();

// Меню
output.WriteMenu("Выберите:",
    ("1", "Опция 1"),
    ("2", "Опция 2"),
    ("0", "Выход")
);

// Список
output.WriteList(new[] { "Item 1", "Item 2" }, item => item);

// Таблица
output.WriteTable(data, row => row.ToString());
```

## 🔄 Типичные сценарии использования

### Сценарий 1: Простой диалог

```csharp
var io = new IOFacade();
string answer = io.AskText("Ваш вопрос?");
io.ShowSuccess($"Вы ответили: {answer}");
```

### Сценарий 2: Валидация данных

```csharp
var io = new IOFacade();

string email;
while (true)
{
    email = io.AskText("Email:");
    if (email.Contains("@"))
        break;
    io.ShowError("Email должен содержать @");
}
```

### Сценарий 3: Интерактивное меню

```csharp
var io = new IOFacade();

while (true)
{
    io.ShowSeparator("Главное меню");
    var choice = io.AskSelection("Выберите:", 
        new[] { "Добавить", "Удалить", "Выход" });
    
    if (choice == "Выход") break;
    
    io.ShowInfo($"Вы выбрали: {choice}");
}
```

### Сценарий 4: Обработка ошибок

```csharp
var io = new IOFacade();

try
{
    // Код который может вызвать ошибку
    throw new Exception("Критическая ошибка");
}
catch (Exception ex)
{
    io.ShowException(ex);
    io.ShowWarning("Операция не выполнена");
}
```

## 🎨 Цветовая схема

Рекомендуемые цвета для разных типов сообщений:

| Тип | Метод | Цвет | Использование |
|-----|-------|------|---------------|
| Успех | `WriteSuccess()` | 🟢 Зелёный | Операция успешно выполнена |
| Предупреждение | `WriteWarning()` | 🟡 Жёлтый | Важная информация, нужна внимательность |
| Ошибка | `WriteError()` | 🔴 Красный | Критическая ошибка, операция не выполнена |
| Информация | `WriteInfo()` | 🔵 Голубой | Нейтральная информация |

## 📖 Документация

Полная документация по использованию:

1. **[USAGE_GUIDE.md](USAGE_GUIDE.md)** - подробное руководство со всеми примерами
2. **[INPUT_OUTPUT_ARCHITECTURE.md](INPUT_OUTPUT_ARCHITECTURE.md)** - описание архитектуры
3. **[ARCHITECTURE_DIAGRAMS.md](ARCHITECTURE_DIAGRAMS.md)** - визуальные диаграммы
4. **[IMPLEMENTATION_REPORT.md](IMPLEMENTATION_REPORT.md)** - отчет о реализации
5. **[CHECKLIST.md](CHECKLIST.md)** - чек-лист всех компонентов

## 💡 Примеры

### Примеры базовых операций
Смотрите [Output/InputOutputExamples.cs](Output/InputOutputExamples.cs)
- Консольный вывод
- Консольный ввод
- Полный цикл работы

### Примеры адаптеров
Смотрите [Examples/AdapterExamples.cs](Examples/AdapterExamples.cs)
- Использование IOFacade
- Работа только с вводом/выводом
- Комплексные формы
- Обработка ошибок
- Интерактивные меню

## 🔌 Интеграция в приложение

### Пример интеграции в главный класс

```csharp
using Presentation.Adapters;

public class Application
{
    private readonly IOFacade _io;

    public Application()
    {
        _io = new IOFacade();
    }

    public void Run()
    {
        _io.ShowSeparator("Главное меню");
        
        string name = _io.AskText("Введите имя:");
        _io.ShowSuccess($"Привет, {name}!");
    }
}

// Использование
var app = new Application();
app.Run();
```

## 🏗️ Архитектурные принципы

1. **Разделение ответственности** - Input и Output независимы
2. **Интерфейсы как контракты** - передавайте интерфейсы, не реализации
3. **Фасад для упрощения** - IOFacade для частых операций
4. **Адаптеры для специализации** - специализированные адаптеры для особых случаев
5. **Расширяемость** - легко добавлять новые реализации

## 🚀 Расширение системы

Хотите добавить новый способ ввода/вывода? Просто реализуйте интерфейсы:

```csharp
// Пример: вывод в файл
public class FileOutput : IErrorOutput
{
    private readonly string _filePath;
    
    public FileOutput(string filePath) => _filePath = filePath;
    
    // Реализуйте методы интерфейсов
    public void WriteText(string message) => File.AppendAllText(_filePath, message);
    // ... остальные методы
}

// Использование
var output = new FileOutput("log.txt");
output.WriteSuccess("Это запишется в файл!");
```

## 📊 Статистика слоя Presentation

| Компонент | Количество |
|-----------|-----------|
| Интерфейсов | 8 |
| Реализаций | 2 |
| Адаптеров/Фасадов | 3 |
| Основных методов | 150+ |
| Файлов документации | 5 |

## ✅ Проверка работоспособности

Проект успешно собирается:
```
Presentation net10.0 успешно выполнено
```

Все компоненты готовы к использованию.

## 🎓 Рекомендации

1. **Начните с IOFacade** для быстрого старта
2. **Переходите к интерфейсам** когда нужна более точная контроль
3. **Используйте адаптеры** для специализированных задач
4. **Следуйте цветовой схеме** для консистентности
5. **Обрабатывайте исключения** и выводите через `WriteDetailedException()`

---

**Слой готов к использованию! 🎉**

Начните с фасада IOFacade и постепенно переходите к более специализированным интерфейсам по мере необходимости.
