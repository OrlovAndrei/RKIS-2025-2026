<UserControl x:Class="TodoApp.Desktop.Views.TodoListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="236"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1C2430" Padding="22">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="TodoApp" Foreground="White" FontSize="26" FontWeight="Bold"/>
                    <TextBlock Text="Desktop" Foreground="#94A3B8" FontSize="13" Margin="0,4,0,28"/>

                    <Border Background="#273244" CornerRadius="6" Padding="12" Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock Text="Профиль" Foreground="#94A3B8" FontSize="12"/>
                            <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#22314A" CornerRadius="6" Padding="12">
                        <StackPanel>
                            <TextBlock Text="Работа с задачами" Foreground="White" FontWeight="SemiBold"/>
                            <TextBlock Text="Поиск, статусы, добавление и редактирование" Foreground="#CBD5E1" TextWrapping="Wrap" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <TextBlock DockPanel.Dock="Bottom"
                           Text="SQLite + EF Core"
                           Foreground="#94A3B8"
                           VerticalAlignment="Bottom"/>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="26">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,18">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="Список задач" FontSize="30" FontWeight="SemiBold"/>
                    <TextBlock Text="Управляйте задачами текущего профиля" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Content="Обновить" Command="{Binding RefreshCommand}" DockPanel.Dock="Right" VerticalAlignment="Top"/>
            </DockPanel>

            <Border Grid.Row="1" Background="{StaticResource PanelBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="18"/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="18"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Поиск" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Статус" FontWeight="SemiBold"/>
                        <ComboBox SelectedItem="{Binding StatusFilter}" ItemsSource="{Binding Statuses}"/>
                    </StackPanel>
                    <Button Grid.Column="4" Content="Сбросить" Command="{Binding ClearFilterCommand}" VerticalAlignment="Bottom" Margin="0,0,0,12"/>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="22"/>
                    <ColumnDefinition Width="330"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource PanelBrush}" CornerRadius="8" Padding="1">
                    <DataGrid ItemsSource="{Binding TasksView}"
                              SelectedItem="{Binding SelectedTask}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="72"/>
                            <DataGridTextColumn Header="Задача" Binding="{Binding Text}" Width="*"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="140"/>
                            <DataGridTextColumn Header="Обновлено" Binding="{Binding LastUpdate, StringFormat=yyyy-MM-dd HH:mm}" Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="18"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="18" Background="{StaticResource PanelBrush}" CornerRadius="8">
                        <StackPanel DataContext="{Binding AddForm}">
                            <TextBlock Text="Добавить задачу" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="Текст" FontWeight="SemiBold" Margin="0,14,0,0"/>
                            <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="92" TextWrapping="Wrap"/>
                            <TextBlock Text="Статус" FontWeight="SemiBold"/>
                            <ComboBox SelectedItem="{Binding Status}" ItemsSource="{Binding Statuses}"/>
                            <Button Content="Добавить" Command="{Binding DataContext.AddTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource PrimaryButton}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Padding="18" Background="{StaticResource PanelBrush}" CornerRadius="8">
                        <StackPanel DataContext="{Binding EditForm}">
                            <TextBlock Text="Выбранная задача" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="Текст" FontWeight="SemiBold" Margin="0,14,0,0"/>
                            <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="92" TextWrapping="Wrap"/>
                            <TextBlock Text="Статус" FontWeight="SemiBold"/>
                            <ComboBox SelectedItem="{Binding Status}" ItemsSource="{Binding Statuses}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Content="Сохранить" Command="{Binding DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource PrimaryButton}"/>
                                <Button Grid.Column="2" Content="Удалить" Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource DangerButton}"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

            <Border Grid.Row="3" Background="#E8F5EE" CornerRadius="6" Padding="12" Margin="0,14,0,0">
                <TextBlock Text="{Binding Message}" Foreground="{StaticResource SuccessBrush}"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
