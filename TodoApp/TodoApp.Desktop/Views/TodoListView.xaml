<UserControl x:Class="TodoApp.Desktop.Views.TodoListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:TodoApp.Models;assembly=TodoApp.Models">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="{Binding Title}" FontSize="26" FontWeight="SemiBold" DockPanel.Dock="Left"/>
            <Button Content="Обновить" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center" Margin="0,0,12,0"/>
            <ComboBox Grid.Column="1" SelectedItem="{Binding StatusFilter}" ItemsSource="{Binding Statuses}" Margin="0,0,12,0"/>
            <Button Grid.Column="2" Content="Сбросить" Command="{Binding ClearFilterCommand}"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding TasksView}"
                      SelectedItem="{Binding SelectedTask}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="70"/>
                    <DataGridTextColumn Header="Text" Binding="{Binding Text}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140"/>
                    <DataGridTextColumn Header="LastUpdate" Binding="{Binding LastUpdate, StringFormat=yyyy-MM-dd HH:mm}" Width="160"/>
                </DataGrid.Columns>
            </DataGrid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="18"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="18" Background="White" CornerRadius="6">
                    <StackPanel DataContext="{Binding AddForm}">
                        <TextBlock Text="Новая задача" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <TextBlock Text="Текст"/>
                        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="86"/>
                        <TextBlock Text="Статус"/>
                        <ComboBox SelectedItem="{Binding Status}" ItemsSource="{Binding Statuses}"/>
                        <Button Content="Добавить" Command="{Binding DataContext.AddTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Background="#1F6FEB" Foreground="White"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Padding="18" Background="White" CornerRadius="6">
                    <StackPanel DataContext="{Binding EditForm}">
                        <TextBlock Text="Редактирование" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <TextBlock Text="Текст"/>
                        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="86"/>
                        <TextBlock Text="Статус"/>
                        <ComboBox SelectedItem="{Binding Status}" ItemsSource="{Binding Statuses}"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Сохранить" Command="{Binding DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Background="#20744A" Foreground="White"/>
                            <Button Content="Удалить" Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Background="#B3261E" Foreground="White"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <TextBlock Grid.Row="3" Text="{Binding Message}" Margin="0,12,0,0" Foreground="#31572C"/>
    </Grid>
</UserControl>
