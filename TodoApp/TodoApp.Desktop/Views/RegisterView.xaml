<UserControl x:Class="TodoApp.Desktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Border Width="520" Padding="32" Background="{StaticResource PanelBrush}" CornerRadius="8"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="22" ShadowDepth="2" Opacity="0.16"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Регистрация" FontSize="28" FontWeight="SemiBold"/>
                <TextBlock Text="Создайте профиль для хранения задач" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,22"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Логин" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Пароль" FontWeight="SemiBold"/>
                        <PasswordBox PasswordChanged="PasswordBox_OnPasswordChanged"/>
                    </StackPanel>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Имя" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Фамилия" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Год рождения" FontWeight="SemiBold"/>
                <TextBox Text="{Binding BirthYear, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="{Binding Message}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,0,0,12"/>

                <Button Content="Зарегистрироваться" Command="{Binding RegisterCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,10"/>
                <Button Content="Назад ко входу" Command="{Binding ShowLoginCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
