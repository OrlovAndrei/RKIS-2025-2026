<UserControl x:Class="TodoApp.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Border Width="380" Padding="28" Background="White" CornerRadius="6"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.12"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Вход в TodoApp" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,18"/>

                <TextBlock Text="Логин"/>
                <TextBox Text="{Binding LoginText, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Пароль"/>
                <PasswordBox PasswordChanged="PasswordBox_OnPasswordChanged"/>

                <TextBlock Text="{Binding Message}" Foreground="#B3261E" TextWrapping="Wrap" Margin="0,4,0,10"/>

                <Button Content="Войти" Command="{Binding LoginCommand}" Background="#1F6FEB" Foreground="White"/>
                <Button Content="Создать профиль" Command="{Binding ShowRegisterCommand}" Background="#EEF2F7"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
