<UserControl x:Class="TodoApp.Desktop.Views.AddTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Padding="32" Margin="32" Background="White" CornerRadius="16">
        <StackPanel MaxWidth="520">
            <TextBlock Text="AddTaskViewModel"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="0,0,0,24" />
            <TextBlock Text="Текст задачи" />
            <TextBox Text="{Binding TaskText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     Height="140"
                     Margin="0,4,0,12" />
            <TextBlock Text="Статус" />
            <ComboBox ItemsSource="{Binding AvailableStatuses}"
                      SelectedItem="{Binding SelectedStatus}"
                      Margin="0,4,0,16" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="16,8" Margin="0,0,12,0" />
                <Button Content="Отмена" Command="{Binding CancelCommand}" Padding="16,8" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
