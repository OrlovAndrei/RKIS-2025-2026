<UserControl x:Class="TodoApp.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Padding="32" Margin="32" Background="White" CornerRadius="16">
        <StackPanel MaxWidth="420">
            <TextBlock Text="LoginViewModel"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="0,0,0,24" />
            <TextBlock Text="Логин" />
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <TextBlock Text="Пароль" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <TextBlock Text="{Binding StatusMessage}" Margin="0,0,0,16" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Войти" Command="{Binding LoginCommand}" Padding="16,8" Margin="0,0,12,0" />
                <Button Content="Регистрация" Command="{Binding OpenRegisterCommand}" Padding="16,8" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
