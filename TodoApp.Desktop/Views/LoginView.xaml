<UserControl x:Class="TodoApp.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="320">
    
    <StackPanel Width="320" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
        <TextBlock Text="Вход в профиль" FontSize="22" FontWeight="SemiBold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <TextBlock Text="Логин:" Margin="0,0,0,4"/>
        <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,12" Padding="6"/>
        
        <TextBlock Text="Пароль:" Margin="0,0,0,4"/>
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,15" Padding="6"/>
        
        <Button Content="Войти" Command="{Binding LoginCommand}" 
                Margin="0,0,0,10" Padding="10,6" FontSize="14"
                IsDefault="True"/>
        
        <Button Content="Нет аккаунта? Регистрация" 
                Command="{Binding GoToRegisterCommand}" 
                Background="Transparent" BorderBrush="Transparent"
                Foreground="Blue" Cursor="Hand"
                HorizontalAlignment="Center" Margin="0,0,0,15"/>
                
        <TextBlock Text="{Binding Error}" Foreground="Red" TextWrapping="Wrap" 
                   HorizontalAlignment="Center" FontSize="13"/>
    </StackPanel>
</UserControl>