<UserControl x:Class="TodoApp.Desktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Padding="32" Margin="32" Background="White" CornerRadius="16">
        <StackPanel MaxWidth="420">
            <TextBlock Text="RegisterViewModel"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="0,0,0,24" />
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
            <TextBox Text="{Binding BirthYear, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
            <TextBlock Text="{Binding StatusMessage}" Margin="0,0,0,16" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Создать профиль" Command="{Binding RegisterCommand}" Padding="16,8" Margin="0,0,12,0" />
                <Button Content="Назад" Command="{Binding BackToLoginCommand}" Padding="16,8" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
