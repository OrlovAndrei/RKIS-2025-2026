<UserControl x:Class="TodoApp.Desktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:TodoApp.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:RegisterViewModel}">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
        <TextBlock Text="Регистрация" FontSize="20" Margin="0,0,0,20"/>
        <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
        <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,5"/>
        <TextBox Text="{Binding FirstName}" Margin="0,5"/>
        <TextBox Text="{Binding LastName}" Margin="0,5"/>
        <TextBox Text="{Binding BirthYear, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,5"/>
        <Button Content="Зарегистрироваться" Command="{Binding RegisterCommand}" Margin="0,10"/>
        <Button Content="Отмена" Command="{Binding CancelCommand}" />
    </StackPanel>
</UserControl>