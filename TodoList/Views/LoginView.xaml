<UserControl x:Class="TodoApp.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <TextBlock Text="Вход в систему" FontSize="20" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="Логин" Margin="0,0,0,4"/>
            <TextBox Margin="0,0,0,10" Padding="10" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Пароль" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10" Padding="10"/>
            <Button Content="Войти" Margin="0,10,0,5" Padding="10" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButton}"/>
            <Button Content="Регистрация" Margin="0,5,0,10" Padding="10" Command="{Binding NavigateToRegisterCommand}" Style="{StaticResource SecondaryButton}"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}"/>
            <ProgressBar IsIndeterminate="True" Height="4" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>
    </Grid>
</UserControl>
